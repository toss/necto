{
  "name": "@necto-plugin/__PLUGIN_ID__",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite",
    "test": "npm run typecheck",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@necto/bridge": "__NECTO_BRIDGE_SPEC__"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.0"
  }
}
